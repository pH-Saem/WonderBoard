<!DOCTYPE html>
<html lang="en">
	<head>
		<link rel="stylesheet" href="/css/styles.css" />
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>WonderBoard</title>
	</head>
	<body>
		<img class="banner" src="/banner.png" />
		<main>
			<div class="side-bar">
				<form class="side-bar__login">
					<div class="side-bar__login__input">
						<label for="id">ID</label>
						<input id="id" type="text" placeholder="ID" />
					</div>
					<div class="side-bar__login__input">
						<label for="pw">PW</label>
						<input id="pw" type="password" placeholder="PW" />
					</div>
					<button class="side-bar__login__btn">Login</button>
				</form>
				<button class="side-bar__write">글쓰기</button>
				<form class="side-bar__search">
					<input placeholder="Search..." type="text" />
					<button class="side-bar__search__btn">Search</button>
				</form>
				<ul class="side-bar__board-list">
					<% for (let i = 0; i < boards.length; i++) { let
					url='/board/'+i; %>
					<li><a href="<%= url%>"><%= boards[i].board_name %></a></li>
					<% } %>
				</ul>
			</div>
			<div class="board">
				<h1 class="board__title"><%= boards[category].board_name %></h1>
				<div class="board__content">
					<% for(let i = 0; i < data.length; i++) {%>
					<div class="board__content__row">
						<span><%= data[i].post_title%></span>
						<span><%= data[i].post_date%></span>
						<span><%= data[i].post__writer%></span>
					</div>
					<% } %>
				</div>
				<div class="board__pagenum">
					<span>1</span>
					<span>2</span>
					<span>3</span>
				</div>
			</div>
		</main>
	</body>
</html>
